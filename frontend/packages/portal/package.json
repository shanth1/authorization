{
  "name": "@frontend/portal",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "VITE_PROJECT=portal vite --config ../../vite.config.ts",
    "build": "tsc -b && VITE_PROJECT=portal vite build --config ../../vite.config.ts",
    "lint": "eslint .",
    "preview": "VITE_PROJECT=portal vite preview --config ../../vite.config.ts"
  },
  "dependencies": {
    "@frontend/shared": "workspace:*"
  }
}
