{
  "name": "sso",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "VITE_PROJECT=sso vite --config ../../vite.config.ts",
    "build": "tsc -b && VITE_PROJECT=sso vite build --config ../../vite.config.ts",
    "lint": "eslint .",
    "preview": "VITE_PROJECT=sso vite preview --config ../../vite.config.ts"
  },
  "dependencies": {
    "@common": "workspace:*"
  }
}
